# Copy file này thành .env và điền giá trị thật

# --- Gemini AI: chọn 1 trong 2 cách ---
# Cách 1: Gemini API (Google AI Studio) - dùng API key
# GEMINI_API_KEY=your_gemini_api_key   # Lấy tại https://aistudio.google.com/apikey

# Cách 2: Vertex AI (bắt buộc đủ 4 biến)
# USE_VERTEX_AI=true
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_CLOUD_LOCATION=us-central1
# VERTEX_AI_SERVICE_ACCOUNT_PATH=đường/dẫn/đến/service-account.json
# VERTEX_AI_ENDPOINT_ID=id-endpoint-trong-vertex-ai
# VERTEX_AI_API_KEY=your_vertex_ai_api_key

# Tùy chọn: tên model (khi dùng endpoint thì bỏ qua; mặc định: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# PostgreSQL
# Cách 1: Connection string (Neon, Supabase, Vercel Postgres)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# Cách 2: Biến rời
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=camguard_pro
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_SSL=false   # Đặt false nếu DB không hỗ trợ SSL
